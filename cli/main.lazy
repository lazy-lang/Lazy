
struct Person {
    name: str,
    age: i8,

    static create: fn(name: str, age: i8) -> Person {
        new Person { name, age };
    },

    age_up: fn() {
        self.age += 1;
    }

}

main {
    let me = Person::create::test<A>("Google", 19);
    me.age_up();
    print(me);
}